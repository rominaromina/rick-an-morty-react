import React from "react";

function CharacterPlaceholder({ name }) {
  return (
     <h1 id="character-name-placeholder" className="character-name-placeholder"> {name}</h1>
  );
}

export default CharacterPlaceholder;
